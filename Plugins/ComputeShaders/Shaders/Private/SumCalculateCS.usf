#include "/Engine/Public/Platform.ush"

// 每个线程组输出一个和
RWBuffer<float> GroupSums;

Texture2D<float4> InputTexture;
float Value1;
float Value2;
uint2 CachedRenderTargetSize;

groupshared float SharedSums[1024];

[numthreads(1024, 1, 1)]
void MainComputeShader(uint3 GroupID : SV_GroupID, uint GroupIndex : SV_GroupIndex)
{

    GroupSums[GroupID.x] = (float)InputTexture.Load(int3(0, 0, 0)).r;

}